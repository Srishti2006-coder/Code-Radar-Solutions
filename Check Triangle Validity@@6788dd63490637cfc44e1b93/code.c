#include <stdio.h>

int main() {
    int a,b,c;
    scanf("%d", "%d", "%d");
    if((a+b>c) ||(b+c>a) || (c+a>a)){
        printf("Valid");
    }
    else if {
        printf("Invalid");
    }
    return 0;
}