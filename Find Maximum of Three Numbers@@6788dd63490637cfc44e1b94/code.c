#include <stdio.h>

int main() {
    int a;
    int b;
    scanf("%d", &a);
    scanf("%d", &b);
    if (a>b) {
        printf("maximum number");
    }
    else {
        printf("not maximum number");
    }
    return 0;
}